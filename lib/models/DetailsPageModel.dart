class DetailsScreenArguments {
  final WWW;
  final HTTP;
  final SSDP;
  final DOMAIN;
  final HTTPS;
  final others;
  final DNS;
  DetailsScreenArguments(this.WWW, this.HTTP, this.HTTPS, this.SSDP, this.DOMAIN, this.DNS, this.others);
}
